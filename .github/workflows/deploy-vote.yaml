name: Deploy-Vote
on:
  push:
    branches:
      - master
      
jobs:
  Clone-to-ubuntu:
    name: Clone repo on ubuntu
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: cp -rf ./voting-app ~/app